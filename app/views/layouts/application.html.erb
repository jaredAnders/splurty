<!DOCTYPE html>
<html>
<head>
  <title>Splurty</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <div class="col-xs-3">
      <%= link_to root_path, class: 'logo' do %>
        <div class="circle">
          <br />
          Fresh
          <br />
          Quotes
        </div>
      <% end %>
    </div>
    <!-- <div class="col-xs-4">
       Droppin the freshest quotes in the game
    </div> -->
    <div class="col-xs-6 col-xs-offset-3 links">
      <%= link_to '+ add a quote', '#', data: {toggle: 'modal', target: '#newQuoteModal'}%>
      &nbsp;/&nbsp;
      <%= link_to 'about', about_path %>
    </div>
  </header>

  <!-- Modal -->
  <div class="modal fade" id="newQuoteModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
   <div class="modal-dialog">
     <div class="modal-content">
       <div class="modal-header">
         <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
         <h4 class="modal-title" id="myModalLabel">Add a fresh quote</h4>
       </div>
       <%= simple_form_for Quote.new do |f| %>
         <div class="modal-body">
         <%= f.input :saying, label: "Quote:", placeholder: "quote" %>
         <br>
         <%= f.input :author, label: "Author:", placeholder: "author" %>
         </div>
         <div class="modal-footer">
           <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
           <%= f.submit '+ add quote', class: 'btn btn-danger' %>
         </div>
       <% end %>
     </div>
   </div>
  </div>


  <hr />
  <br class="clear" />

  <div class="container col-xs-12 col-sm-10 col-sm-offset-1">
    <% if flash[:error].present? %>
      <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
        <%= flash[:error].html_safe %>
      </div>
    <% elsif flash[:success].present? %>
      <div class="col-xs-10 col-xs-offset-1 alert alert-success">
        <%= flash[:success].html_safe %>
      </div>
    <% end %>
    <%= yield %>
  </div>

  <div class="footer">
    <br class="clear" />
    <div class="container">
      <div class="col-xs-12 col-sm-push-6 col-sm-6 next-quote">
        <%= link_to root_path, class: 'btn btn-danger' do %>
          <span>View new quote <span class="glyphicon glyphicon-refresh" aria-hidden="true"></span></span>
        <% end %>
      </div>
      <div class="col-xs-12 col-sm-pull-6 col-sm-6 footer-blurb">
        Built by <a target="_blank" href="https://jaredanders.github.io">Jared Gilbert</a>
      </div>
    </div>
  </div>

</body>
</html>
